const pluginPermissions = {
  accessSettings: [{ action: 'plugin::magic-mark.settings.read', subject: null }],
  readSettings: [{ action: 'plugin::magic-mark.settings.read', subject: null }],
  updateSettings: [{ action: 'plugin::magic-mark.settings.update', subject: null }],
};

export default pluginPermissions;

